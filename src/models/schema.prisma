
datasource db {
  provider = "mongodb"
  url      = env("mongodb+srv://rahatrizvirahman:wHuH8XY2osr2Y0DN@cluster0.7x3kiue.mongodb.net/kompani-dataset")
}

generator client {
  provider = "prisma-client-js"
  previewFeatures = ["metrics"]
  engineType = "binary"
}

model User {
  id              String        @id @default(auto()) @map("_id") @db.ObjectId
  username        String        @unique
  firstName       String
  lastName        String
  email           String
}